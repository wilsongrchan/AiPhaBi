# 只打常用字白名單的「手動強制擋」——common_extra.txt（手動強制留）的反向。
# 這裡列的字會從白名單移除，即使異體回填／甲表／GB 一級把它收了進去。
#
# 用途：異體回填（build_rime.py 的 _keep_variant）靠 OpenCC t2s 的每一條邊當
# 「這是常見傳承寫法」的證據，對一次性的變體字並不成立——像 衕（只出現在「衚衕」）、
# 榘（矩的異體）、査（查的異體）這種，只活在單一冷僻詞裡，卻跟 濕／歎／裏 一樣被放行。
# 沒有乾淨的自動判準（字頻切不開、s2t 迴轉也切不開），所以逐字由 Wilson 認可後列在這裡。
#
# 一行一批、以空白分隔皆可；# 開頭為註解。改動要 flag Side B 重建碼表。
薹 鄖 榘 彔 査 輥
