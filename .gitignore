# 第三方字形資料 —— 各有授權，**不進版控**。跑 fetch_data.py 重建。
#
# ⚠️ 「不進版控」不等於「不對外散布」：公開網站會把 graphics.txt 裡約 1,375 個字的
# 筆畫輪廓抽出來，隨 GitHub Pages 發佈出去（Wilson 於 2026-08-17 決定接受 Arphic
# Public License 的義務）。完整說明見本檔下方 site/.cache/ 那一段。
# 這一行原本寫的是「不隨本專案散布」，在 side-c 併進 main 之前是對的，之後就不是了。
data/graphics.txt
data/dictionary.txt
data/tw_strokes.json

# data/freq.json 從這裡拿掉、改成收進 git（139K）。原因：它是 build_rime.py 的輸入，
# 又不進版控，於是每台機器各有一份，兩台機器輪流建置就會互相蓋掉 8000 多個 weight
# ——碼表內容一模一樣，只有重碼時誰排前面不一樣（實測 1167 個重碼碼位裡有 15 個會換
# 順序，例 JTFC 乘/乖）。不進版控就永遠對不齊，而且新機器沒跑 fetch_data.py 根本建不起來。

# 智能聯想資料庫（官方 librime-predict，各自授權）：由 fetch_data.py 下載
data/predict.db
data/predict.txt

# data/opencc.json（繁簡對照，OpenCC，Apache-2.0）跟 freq.json 一樣收進 git（115K）：
# 它也是 build_rime.py 的輸入（打繁出簡／打簡出繁的對照表就是它），不進版控就會兩台
# 機器各有一份、各建各的。授權是 Apache-2.0，隨專案散布沒問題。
# 收了這份之後，Side B 的建置所需檔案全部在版控裡，新機器 clone 完就能直接建。

# 香港教育局筆順快取：EDB 版權內容，只作個人參考，不上傳。
data/hk_cache.json

# 自動備份與雜項
data/backups/
__pycache__/
node_modules/
.DS_Store

# HEIC 原檔（已有 jpg 版本）
*.heic

# 官方倉頡碼表（rime-cangjie，GPL）：由 fetch_data.py 下載
data/cangjie.json

# 倉頡拆碼對應（由倉頡字典.com 的拆碼圖推出，僅供個人參考）
data/cangjie_map.json
data/cj_img/

# session side declaration (per-checkout, never committed)
.aiphabi-side

# 公開網站的產生檔（Side C）：由 site/tools/build_site_data.py 從 data/ 產生，
# 部署時在 CI 裡重跑。不進版控是刻意的 —— 收進來就會有一份跟 codes.json 不同步的碼表
# 躺在 repo 裡，而「網站上的字跟輸入法不一樣」是最難發現的那種錯。
# 本機預覽前先跑：python3 site/tools/build_site_data.py
site/assets/dict.json
site/assets/t2s.json
site/assets/zigen.json
site/assets/principles.json
site/assets/jianma.json
site/assets/conventional.json
# 詞組頁：詞組連打／四碼快打的例詞。碼是建置時現算、再拿 rime/ 的出貨碼表對過的，
# 更沒有理由收進版控 —— 收了就會有一份跟碼表對不上的碼躺在 repo 裡。
site/assets/phrases.json
# 試打頁的詞庫（3MB，詞組開關打開才抓）。同理不進版控：詞名單取自出貨碼表，
# 但碼是從 codes.json 重算的，收進來就會有一份會過期的碼躺在 repo 裡。
site/assets/phrase_dict.json
site/assets/pinyin.json
# 〈拆碼查詢〉的拼音查詞表，build_pyphrase() 現算
site/assets/pyphrase.json
# 〈字根練習〉考哪幾條字根。手挑的清單是 site/content/lianxi.md（那份**有**進版控），
# 這個檔只是它對回 zigen.json 之後的結果，跟其他產生檔同理不收。
site/assets/lianxi.json
# 拆碼圖＋字形，跟 glyphs.json 同源同授權（Arphic PL），一樣會隨網站發佈出去。
# 跟 pinyin.json 分開是因為下載時機不同：這一份要等使用者點進查字框才抓。
site/assets/pinyin_glyphs.json
# practice.json 是試打頁的參考文章＋它用得到的字形。字形部分跟 glyphs.json
# 同源同授權（Arphic PL），一樣會隨網站發佈出去 —— 見本檔下面 site/.cache/ 那段。
site/assets/practice.json

# 網站建置時抓下來的第三方字形資料快取（Arphic PL）——不進版控，跑一次就有。
#
# ⚠️ 跟本檔最上面那條「第三方字形資料…不隨本專案散布」有關，讀那條時要一起讀這裡：
# graphics.txt 本身**仍然不進版控**，那條沒有變。但公開網站的〈字根表〉現在會把
# 其中 1375 個字的筆畫輪廓抽出來（只取 strokes、不取 medians）產生 glyphs.json，
# 而那個檔會**隨網站發佈出去**。也就是說，衍生自 makemeahanzi／Arphic 的資料現在
# 確實會對外散布——只是不是經由 git，而是經由 GitHub Pages。
# Wilson 於 2026-08-17 決定接受 Arphic Public License 的義務以換取字根表能畫出字根。
# 授權三項義務見 site/tools/build_site_data.py 的 build_glyphs()，以及 site/ARPHICPL.txt。
site/.cache/
site/assets/glyphs.json

# 手機版選單要用的章節索引（site/tools/build_nav.py 從十二頁的 HTML 現掃出來，
# 建置時產生，跟其他資料檔一樣不進版控）
site/assets/nav.json

# 內部審查用的對照頁（site/tools/review_sources.py 產生），不進版控也不發佈
site/_review/

# 標誌的 Illustrator 母檔（1.6 MB 的二進位）。PNG 版有進版控，網站用的三個尺寸
# 都是從它切出來的；.ai 留在工作目錄就好。
# ⚠️ 一定要寫在這裡：Side B 的部署腳本會跑 `git add -A`，掃的是整個工作目錄而
# 不只 rime/，沒有這一行的話，隨便一次部署就會順手把這個檔案提交上去。
AiPhaBi Logo.ai

# Vercel CLI 在本機留下的專案連結資訊（含專案 ID），不是設定，不進版控。
.vercel/
